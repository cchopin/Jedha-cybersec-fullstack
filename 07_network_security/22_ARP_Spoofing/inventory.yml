all:
  hosts:
    localhost:
      ansible_connection: local
  vars:
    gns3_server: "http://192.168.190.174:80"
    # Le project_id et template IDs sont gérés dynamiquement
    # par 01_create_topology.yml
